// RETIRED (G-03, fail-closed tombstone). This merged agent-produced enrichment shards ({id:{why,cues,guide}})
// into teach-works.js with no human approval or provenance binding — a raw model-output sink. Model-generated
// teaching content now reaches teach-works.js only via the reviewed path → scripts/curate-merge.mjs --run.
console.error('❌ merge-enrich.mjs is RETIRED (G-03). Agent-produced enrichment shards reach teach-works.js only via scripts/curate-merge.mjs --run (human-approved + hash-bound).');
process.exit(1);
