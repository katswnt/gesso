// RETIRED (G-03, fail-closed tombstone). This regenerated "Ask the guide" Q&A via SONNET AGENTS and wrote their
// output directly into teach-works.js — a raw model-output sink with no human approval or provenance binding.
// Any model-generated teaching content must now go through the reviewed path (broker run → tool-less runner →
// human review → scripts/curate-merge.mjs --run). NOTE: the tool-less pass does not yet regenerate study-guide
// Q&A; rebuilding that on the secure+approved path is a DEFERRED follow-up (see tasks/todo.md).
console.error('❌ guides-regen.mjs is RETIRED (G-03). Model-generated teaching content reaches teach-works.js only via scripts/curate-merge.mjs --run (human-approved). Study-guide Q&A regeneration is a deferred secure-successor task.');
process.exit(1);
