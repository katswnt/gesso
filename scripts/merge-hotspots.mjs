// RETIRED (G-03, fail-closed tombstone). This merged agent/model-derived pin output into teach-works.js +
// hotspots.js WITHOUT approval/provenance. Notes + feature-anchored pins are now part of the single tool-less
// vision pass and reach the game only via human review → scripts/curate-merge.mjs --run (approval- + hash-bound).
console.error('❌ merge-hotspots.mjs is RETIRED (G-03). Pins reach teach-works.js/hotspots.js only via scripts/curate-merge.mjs --run <runDir> (human-approved).');
process.exit(1);
