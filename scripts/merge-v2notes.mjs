// RETIRED (G-03, fail-closed tombstone). This merged agent-produced v2 unified-reveal `notes` arrays into
// teach-works.js with no human approval or provenance binding — a raw model-output sink. Notes now reach
// teach-works.js only via the reviewed path → scripts/curate-merge.mjs --run (human-approved + hash-bound).
console.error('❌ merge-v2notes.mjs is RETIRED (G-03). Agent-produced notes reach teach-works.js only via scripts/curate-merge.mjs --run (human-approved + hash-bound).');
process.exit(1);
