# Project instructions for Claude Code

The collaboration policy for this repository is **[`docs/agent-collaboration.md`](docs/agent-collaboration.md)**.
Read it before working here. This file is only a pointer; the policy document is the single source
of truth.

`AGENTS.md` points at the same document, so Claude Code and Codex work from identical rules.

Scope note: this file is **repository-scoped**. Kat's personal global instructions and any
`CLAUDE.md` in her primary checkout still apply to her own machine; where guidance overlaps,
`docs/agent-collaboration.md` governs work *in this repository*, and Kat's standing instructions
outrank a bounded specification when the two conflict — surface that conflict once rather than
re-litigating it.

Before any vision-pipeline work, read [`docs/PIPELINE.md`](docs/PIPELINE.md). See `AGENTS.md` for
the note on `docs/vision-system.md`, which is not yet on `main`.
